# Week 1 - Problem 1: Purchase

Write a program that calculates the total cost of a purchase, including tax.

## Input

Prompt the user for two values:

- **price**: the price per item (a decimal number)
  - The prompt should be `"Enter price per item: "` (note the trailing space)
- **count**: the number of items purchased (a whole number)
  - The prompt should be `"Enter number of items: "` (note the trailing space)

## Processing

- Assume a fixed tax rate of **7.5%**.
- Calculate the **pre-tax cost**: price times count, **rounded to 2 decimal places**.
- Calculate the **tax paid**: pre-tax cost times the tax rate, **rounded to 2 decimal places**.
- Calculate the **after-tax cost**: pre-tax cost plus tax paid.
- Use the `round` function to round to two decimal places

## Output

Print all three values:

- Pre-tax cost
  - Output string: `"Pre-tax cost="`
- Tax paid
  - Output string: `"Tax paid="`
- After-tax cost
  - Output string: `"After-tax cost="`

Each line has a single space between the output string and the value, as shown in the example below.

## Example

```
Enter price per item: 10
Enter number of items: 3
Pre-tax cost= 30.0
Tax paid= 2.25
After-tax cost= 32.25
```

## Rubric

| Criterion      | Points |
| -------------- | ------ |
| Input handling | 10     |
| Correct output | 20     |
| flake8         | 10     |
| **Total**      | **40** |

## How to Submit

Test your code here: [https://bi3mer.github.io/autograder/cs230/w1p1.html](https://bi3mer.github.io/autograder/cs230/w1p1.html). The autograder shows the grade you will receive plus a summary at the very bottom that you **need** for your submission.

Submit on BrightSpace as a **text submission**: copy and paste your code into the text box, with the autograder summary as a docstring at the top. No file upload, no attachment.

```python
"""
Week 1, Problem 1 Autograder Summary
Score: 38 / 40

- Input handling: 10 / 10
- Correct output: 20 / 20
- flake8: 8 / 10
"""

# ... your submission code here
```

A docstring has to be the first statement, so put it above your code, not below it.

**Failure to include the summary will result in -10 points.**

## Note

I've included the exact strings that you should include in the problem definition. You should copy and paste them to avoid annoying problems like the auto-grader thinking that "Pre-tax cost = " is wrong because it has two extra spaces. Also, I made the fact that you should round bold in the processing section because an answer like `"Tax paid= 2.2500"` would be wrong.

Rounding cuts trailing zeros as well: `round(30.0, 2)` is `30.0`, so the expected first line is `Pre-tax cost= 30.0`, not `Pre-tax cost= 30.00`. Use `round` on this problem, not an f-string format such as `:.2f`.
