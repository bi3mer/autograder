# Week 4 - In-Class Activity 1: Spell It Out

Let's write a program that spells a string out vertically. The user types a string, and the program prints it back one character per line, in order. It insists on getting at least one character: if the user presses Enter without typing anything, it asks again.

## Input

Prompt the user with: `"Enter a string: "`

A valid answer has at least one character. If the user presses Enter without typing anything, ask the same question again, as many times as it takes, until the user enters at least one character.

## Processing

- Loop until the user enters a string that has at least one character
- Loop over the positions in the string and print the character at each position
- Hint: [`len()`](https://www.w3schools.com/python/ref_func_len.asp) gives the number of characters in a string

## Output

Once the loop ends, print one line for every character in the string, in the order they appear. Every character counts: letters, digits, punctuation, all of them.

## Examples

```
Enter a string: hello
h
e
l
l
o
```

Here the user pressed Enter without typing anything the first time, so the question was asked again:

```
Enter a string:
Enter a string: CS230!
C
S
2
3
0
!
```

```
Enter a string: a
a
```

## How to Submit

Grade your program on this page, then press **Copy for submission**. That puts the autograder summary and your code on the clipboard as one paste, in the shape BrightSpace wants it: the summary inside a docstring, a blank line, then your code. Paste the whole thing into the **text submission** box.

```python
"""
w4-i1.py — Autograder Summary
Score: 30.00 / 40

- Compiles without syntax errors: OK
- Input handling: 5 / 5
- Correct output: 15 / 15
- Use of a while loop: 5 / 5
- Use of a for loop over range(): 0 / 5
- Indexing into the string: 0 / 5
- flake8: 5 / 5

AI prompts, one per line (write none if you asked none):
-
-
-
"""

# ... your submission code here
```

A docstring has to be the first statement, so it goes above your code, not below it. **Your grade drops by 50% if the summary is missing, or if it sits anywhere other than a docstring at the top.** These all cost you half the points:

- No summary anywhere in the submission.
- The summary pasted below the code.
- The summary above the code but outside the `"""` quotes.

Fill in the `AI prompts` lines before you submit: one question per line, or `none` if you asked none. They stay inside the docstring, under the summary.

Indentation is yours to protect. BrightSpace's editor drops the leading spaces when you paste, and Python without its indentation does not run. Paste as plain text with `Ctrl+Shift+V` (`Cmd+Shift+V` on a Mac), or put the whole submission in a code block.
