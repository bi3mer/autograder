#!/usr/bin/env bash
# Test before every push, so a broken grader never reaches the Pages workflow.
# Nothing to install first: the tests are plain Node with no dependencies.
#
# Install with: npm run hooks:install
set -euo pipefail

npm test
